rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      // Temporary rule for debugging: allows any authenticated user to read/write.
      // This is not secure for production.
      allow read, write: if request.auth != null;
    }
  }
}
